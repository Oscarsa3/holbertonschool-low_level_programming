#include <stdio.h>

/**
 *
 */
int main(void)
{
	int a;
	unsigned long int b = 0, c = 1, d;

	for (1 = 1; i <= 98; i++)
	{
		d = b + c;
		if (i != 98)
		{
			printf("%lu, ", d);
		}
		else
		{
			printf("%lu\n", d);
		}
		b = c;
		c = d;
	}
	return (0);
}
